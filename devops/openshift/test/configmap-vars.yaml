apiVersion: v1
data:
  SERVER_PORT: "8080"
  MANAGEMENT_SERVER_PORT: "8081"
  SPRING_PROFILES_ACTIVE: "test, init-bootstrap-data, fake, wc-stub, it-stub"
  INTYG_REDIS_PORT: "${redis.service.port}"
  INTYG_REDIS_HOST: "${redis.service.host}"
kind: ConfigMap
metadata:
  name: intygsadmin-test-configmap-envvar
type: Opaque
